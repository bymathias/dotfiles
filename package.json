{
  "name": "dotfiles",
  "version": "0.2.0",
  "description": "Config files to setup a system (macOS/Debian) the way I want it.",
  "author": "Mathias Brouilly",
  "license": "MIT",
  "homepage": "https://github.com/bymathias/dotfiles#readme",
  "repository": {
    "type": "git",
    "url": "git+https://bymathias@github.com/bymathias/dotfiles.git"
  },
  "bugs": {
    "url": "https://github.com/bymathias/dotfiles/issues"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "release": "npm version -m 'chore(release): v%s'",
    "preversion": "npm test",
    "version": "conventional-changelog -p angular -i CHANGELOG.md -s && git add --all",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^1.2.0",
    "cz-conventional-changelog": "^1.2.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
