#!/usr/bin/env bash

# Enable Bash completions
# On Arch install: `pacman -S bash-completion`
# On Debian install: `apt-get install bash-completion`
__source_exists "/usr/share/bash-completion/bash_completion"
